## Tests

- Ensure it respects // arrange assert, act
- Use whenever possible typing, avoid `as any` except for mock data (add a TODO every time Claude use it)
- Use jest.spyOn
- Order : beforeAll, beforeEach, afterEach, afterAll




# Unit tests

